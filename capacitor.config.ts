import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pankaj.myapp',
  appName: 'My Angular App',
  webDir: 'dist/my-first-angular-app/browser',
};

export default config;